import { TrackScrolDirective } from './track-scrol.directive';

describe('TrackScrolDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackScrolDirective();
    expect(directive).toBeTruthy();
  });
});
